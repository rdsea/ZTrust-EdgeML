#!/bin/bash

set -euo pipefail

source "$(dirname "$0")/../scripts/common.sh"

main(){

login_zt {{ ziti_config.ctrl.cloud_ctrl.ctrl_advertised_address }} {{ gke.namespace }}

{% for router in ziti_config.router.edge_router %}
setup_router {{ router.id }} {{ router.id }}_namespace {{ router.edge_router_advertised_address }}
{% endfor %}

}

main"$@"
