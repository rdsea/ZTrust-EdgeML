# .env

# ZT
# Cloud control plane
CLOUD_CTRL_ADVERTISED_ADDRESS="ctrl.cloud.hong3nguyen.com"
CLOUD_CTRL_IP=""
CLOUD_CTRL_IP_INTERNAL=""

CLOUD_ROUTER_ADVERTISED_ADDRESS="router.cloud.hong3nguyen.com"
CLOUD_ROUTER_IP=""
CLOUD_ROUTER_IP_INTERNAL=""

# Edge control plane
EDGE_ROUTER_ADVERTISED_ADDRESS="router.edge.hong3nguyen.com"
EDGE_ROUTER_IP=""

# Edge app 
EDGE_APP_IP=""

# list of sensor ips
SENSOR_IP_S=""


# pass for edge devices
PASS=""

# user and pass for ZT config
ZT_USER=
ZT_PASS=


# CTRL_IP=$(cd cloud-gcp && terraform output -raw controller_ip)
# CLOUD_ROUTER_IP=$(cd cloud-gcp && terraform output -raw controller_ip)
# MESSAGEQ_IP=$(cd cloud-gcp && terraform output -raw messageq_ip)
# JAEGER_IP=$(cd cloud-gcp && terraform output -raw database_ip)

